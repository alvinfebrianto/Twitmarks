import "@testing-library/jest-dom";

// biome-ignore lint/suspicious/noEmptyBlockStatements: matchMedia stub for jsdom
const noop = () => {};

Object.defineProperty(window, "matchMedia", {
  writable: true,
  value: (query: string) => ({
    matches: false,
    media: query,
    onchange: null,
    addListener: noop,
    removeListener: noop,
    addEventListener: noop,
    removeEventListener: noop,
    dispatchEvent: () => false,
  }),
});
